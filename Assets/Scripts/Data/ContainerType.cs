namespace Galatea.Data
{
    // Reserved for the future container-visual flow — the held-item carry pose
    // will branch on this (free hand / tray / bowl). Not yet read at runtime.
    public enum ContainerType
    {
        None,
        Tray,
        Bowl
    }
}
